import QtQuick 2.0
import QtQuick.Layouts 1.0
import QtQuick.Window 2.12

Window {
    width: 600; height: 600
    visible: true

    // Anchors { anchors.fill: parent }
    // Coordinates { anchors.fill: parent }
    Layouts { anchors.fill: parent }
    // Positioners { anchors.fill: parent }
}
